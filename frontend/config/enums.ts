export enum TransactionType {
  Single = 'single',
  Repeating = 'repeating',
}

export enum CycleType {
  Daily = 'daily',
  Weekly = 'weekly',
  Monthly = 'monthly',
  Quarterly = 'quarterly',
  Biannually = 'biannually',
  Yearly = 'yearly',
}
